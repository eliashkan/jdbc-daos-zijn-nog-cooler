package be.vdab.dao;

import be.vdab.CategoryException;
import be.vdab.model.Brewer;

import java.util.List;

public class BrewerDAOImpl implements BrewerDAO {
    @Override
    public List<Brewer> getBrewersByZipCode(int zipcode) throws CategoryException {

        // Declare ArrayList Object to return
        // Instantiate SQL String object
        // Establish connection to database
        // Create Prepared Statement
        // Set values in Prep Stat
        // Execute Prep Stat with SQL
        // Loop over the Result Set
        // For each result create Brewer object + set values
        // Add resulting Brewer object to ArrayList
        // catch -> throw exception

        // Return ArrayList
        return null;
    }
}
